-- Ported from NvChad v2.5 (nvchad/configs/gitsigns.lua) so the config owns its own plugin setup.
-- Copied byte for byte; the Nerd Font glyphs below are the originals.

return {
  signs = {
    delete = { text = "󰍵" },
    changedelete = { text = "󱕖" },
  },
}
